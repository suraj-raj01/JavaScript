// Write a program to print area of triangle.

/*

let a = parseInt(prompt("Enter the base: "));
let b = parseInt(prompt("Enter the height: "));
function area(){
    document.write("area of triangle is: ", (1/2)*a*b)
}
area();

*/

// WAP to add two numbers using function.

let a = parseInt(prompt("Enter the first number: "));
let b = parseInt(prompt("Enter the second number: "));
function sum(){
    document.write("Addtion of ",a," and ",b," numbers: ", a+b);
}
sum();
